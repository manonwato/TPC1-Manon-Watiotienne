# TP1C


Pour compiler faire la commande `make` depuis le terminal.

Lancer le programme avec la commande:

`tpc1 > resultat.txt` 

(ou `tpc1.exe > resultat.txt` sur Windows)

Puis visualiser le résultat en ouvrant le fichier `resultat.txt`.
